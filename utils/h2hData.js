// ============================================================
// utils/h2hData.js
// DEPRECATED. Consolidated into utils/h2h.js.
// Kept as a thin re-export so existing imports keep working.
// ============================================================

const { loadAllGames } = require('./h2h');

module.exports = {
  loadAllGames,
};
